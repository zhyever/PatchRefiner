train_dataloader=dict(
    batch_size=4,
    num_workers=4,
    dataset=dict(
        type='CityScapesDataset',
        mode='train',
        split='./data/cityscapes/train.txt',
        min_depth=1e-3,
        max_depth=250,
        transform_cfg=dict(
            degree=1.0,
            random_crop=True,
            random_crop_size=[256, 512],
            network_process_size=[384, 512])))

val_dataloader=dict(
    batch_size=1,
    num_workers=1,
    dataset=dict(
        type='CityScapesDataset',
        mode='infer',
        with_seg_map=True,
        split='./data/cityscapes/val.txt',
        min_depth=1e-3,
        max_depth=250,
        transform_cfg=dict(
            degree=1.0,
            network_process_size=[384, 512])))