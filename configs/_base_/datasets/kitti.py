train_dataloader=dict(
    batch_size=4,
    num_workers=4,
    dataset=dict(
        type='KittiDataset',
        mode='train',
        data_root='./data/kitti',
        split='/ibex/ai/home/liz0l/codes/ZoeDepth/train_test_inputs/kitti_eigen_train_files_with_gt.txt',
        min_depth=1e-3,
        max_depth=80,
        transform_cfg=dict(
            degree=1.0,
            random_crop=True,
            random_crop_size=[176, 304],
            network_process_size=[384, 512])))

val_dataloader=dict(
    batch_size=1,
    num_workers=1,
    dataset=dict(
        type='KittiDataset',
        mode='infer',
        data_root='./data/kitti',
        split='/ibex/ai/home/liz0l/codes/ZoeDepth/train_test_inputs/kitti_eigen_test_files_with_gt.txt',
        min_depth=1e-3,
        max_depth=80,
        transform_cfg=dict(
            degree=1.0,
            network_process_size=[384, 512])))